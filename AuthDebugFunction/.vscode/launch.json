{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to .NET Functions (Pick Process)",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:azureFunctions.pickProcess}"
        }
    ]
}